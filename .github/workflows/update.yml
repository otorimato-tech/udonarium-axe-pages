name: Update Udonarium Axe

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Pages repo
        uses: actions/checkout@v3

      - name: Get latest release info
        id: release
        uses: actions/github-script@v6
        with:
          script: |
            const res = await github.rest.repos.getLatestRelease({
              owner: "xelltis",
              repo: "udonarium_axe"
            });
            core.setOutput("url", res.data.assets[0].browser_download_url);

      - name: Download latest build
        run: |
          curl -L ${{ steps.release.outputs.url }} -o axe.zip

      - name: Unzip build
        run: |
          rm -rf axe_dist
          mkdir axe_dist
          unzip axe.zip -d axe_dist

      - name: Find dist folder
        id: finddist
        run: |
          DIST_PATH=$(find axe_dist -type d -name "dist" | head -n 1)
          echo "DIST_PATH=$DIST_PATH" >> $GITHUB_OUTPUT

      - name: Patch backend URL
        run: |
          sed -i 's#"url": ""#"url": "https://udonarium-backend.otorimato.workers.dev"#' \
            ${{ steps.finddist.outputs.DIST_PATH }}/assets/config.json

      - name: Replace Pages content
        run: |
          rm -rf *
          cp -r ${{ steps.finddist.outputs.DIST_PATH }}/* .

      - name: Commit and push
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Auto update Axe" || echo "No changes to commit"
          git push
